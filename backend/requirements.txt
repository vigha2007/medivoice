Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-Cors==4.0.1
Flask-JWT-Extended==4.6.0
Werkzeug==3.0.3
APScheduler==3.10.4
pyttsx3==2.90
# NOTE (Linux only): pyttsx3's espeak driver requires the `espeak-ng`
# system package (NOT the older `espeak`) — install with:
#   sudo apt-get install espeak-ng
# On macOS/Windows, pyttsx3 uses the built-in OS voices and needs no
# extra system package.
# NOTE: SpeechRecognition/PyAudio were removed — speech-to-text happens
# client-side via the browser's Web Speech API (see voice.py docstring).
# They are not imported anywhere in this backend, and PyAudio fails to
# build in most PaaS environments without system portaudio headers.
reportlab==4.2.2
openpyxl==3.1.5
qrcode==7.4.2
python-dotenv==1.0.1
gunicorn==23.0.0
psycopg2-binary==2.9.9
